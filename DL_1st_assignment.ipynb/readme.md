# Core Idea
this project is about predicting the track popularity based on numberical and categorical features 
the whole challenge were about the preprocessing phase specially feature engineering as i needed a feature transformation for the outliers also the encoding for the necessary categorical columns
, feature extraction and selection 
also preeventing the model from overfitting using BatchNormalization 
# for future works 
i need to play in the preprocessing phase and trying to fill the nulls , transform the outliers in a smarter way , drop unnessary categorical columns 
also using Dropout , Early stopping for preventing the model from overfitting 
(this notebook has been submitted on kaggle and achieved loss = 718 so it still needs modifications) the lowest loss/penalty was 199.6
